{
  "name": "url-shortener",
  "description": "Simple URL shortener",
  "repository": "git@github.com:schoettler/url-shortener.git",
  "license": "MIT",
  "scripts": {
    "start": "yarn start:client && yarn start:server",
    "start:server": "cd server && yarn start",
    "start:client": "cd client && yarn build",
    "lint": "standard server/**/*.js client/**/*.js"
  },
  "private": true
}
